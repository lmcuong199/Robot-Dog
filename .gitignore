# ESP-IDF build output (regenerated by `idf.py build`)
build/

# Managed components — auto-downloaded from dependencies.lock by the component manager
managed_components/

# sdkconfig backup files
sdkconfig.old
